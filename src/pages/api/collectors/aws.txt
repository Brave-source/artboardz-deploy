sudo apt update
sudo apt install nginx
check if nginx is install by clicking on the ip address
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install nodejs
sudo npm install pm2@latest -g
